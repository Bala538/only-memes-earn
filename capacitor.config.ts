import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.onlymemes.app',
  appName: 'Only Memes Earn',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
